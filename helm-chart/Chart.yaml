apiVersion: v2
name: gentepede-app
description: Generated by Gentepede MCP
type: application
version: 0.1.0
appVersion: "1.0.0"
# Note: Chart.yaml is NOT a Helm template — {{ }} syntax is invalid here.
# appVersion is a static field. The actual image tag is set via values.yaml.
